import React, {
    Component,
} from 'react';

import NavbarApp from '@components/header/navbar';

export default class Footer extends Component {
    render() {
        return (
            <header>
                <NavbarApp />
            </header>
        );
    }
}
