import React, {
    Component,
} from 'react';

import Cards from '@components/main/contents/search/cards';

export default class Search extends Component {
    render() {
        return (
            <>
                <Cards />
            </>
        );
    }
}
